{
    "tasks": [
        {
            "type": "shell",
            "label": "Build all & Run (WINDOWS)",
            "command": "${workspaceFolder}\\build_engine.bat; ${workspaceFolder}\\build_game.bat; ${workspaceFolder}\\game.exe",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Build all & Run (LINUX)",
            "command": "bash ${workspaceFolder}/build_engine.sh; bash ${workspaceFolder}/build_game.sh; LD_LIBRARY_PATH=${workspaceFolder} ${workspaceFolder}/game",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Build engine & Run (WINDOWS)",
            "command": "${workspaceFolder}\\build_engine.bat; ${workspaceFolder}\\game.exe",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Build engine & Run (LINUX)",
            "command": "bash ${workspaceFolder}/build_engine.sh; LD_LIBRARY_PATH=${workspaceFolder} ${workspaceFolder}/game",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Build game & Run (WINDOWS)",
            "command": "${workspaceFolder}\\build_game.bat; ${workspaceFolder}\\game.exe",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Build game & Run (LINUX)",
            "command": "bash ${workspaceFolder}/build_game.sh; LD_LIBRARY_PATH=${workspaceFolder} ${workspaceFolder}/game",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Clean all (WINDOWS)",
            "command": "${workspaceFolder}\\clean_all.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Clean all (LINUX)",
            "command": "bash ${workspaceFolder}/clean_all.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Clean engine build (WINDOWS)",
            "command": "${workspaceFolder}\\clean_engine_build.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Clean engine build (LINUX)",
            "command": "bash ${workspaceFolder}/clean_engine_build.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Build Engine(DLL) (WINDOWS)",
            "command": "${workspaceFolder}\\build_engine.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Build Engine(SO) (LINNUX)",
            "command": "bash ${workspaceFolder}/build_engine.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            }
            
        },
        {
            "type": "shell",
            "label": "Build Game (WINDOWS)",
            "command": "${workspaceFolder}\\build_game.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Build Game (LINUX)",
            "command": "bash ${workspaceFolder}/build_game.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "Run game (WINDOWS)",
            "command": "${workspaceFolder}\\game.exe",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell", 
            "label": "Run game (LINUX)",
            "command": "LD_LIBRARY_PATH=${workspaceFolder} ${workspaceFolder}/game",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}
